RATIOS Programs — Pixel-Match Build

This build intentionally uses the APPROVED PROGRAMS MOCKUP itself as the page artwork,
rather than attempting to redraw the approved visual with CSS. That makes the rendered
page visually match the approved image at its native 1026×1532 proportions.

Transparent responsive link hotspots are layered over navigation, hero buttons,
program Learn More areas, CTA buttons, and key footer links so the page remains usable.

Files to add:
  programs.html
  assets/images/programs-approved-page.png

Do NOT add the previous programs.css from the failed reconstruction.
